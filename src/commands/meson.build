power = shared_module(
    'power', 
    'power.cpp', 
    dependencies: [wayfire, jsoncpp],
    install: true, install_dir: wayfire.get_variable(pkgconfig: 'plugindir'), include_directories : incdir)
